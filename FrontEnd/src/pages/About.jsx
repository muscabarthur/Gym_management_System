import { useEffect, useState } from "react";
import { websiteApi } from "../api/api.js";
export default function About(){const [about,setAbout]=useState(null);useEffect(()=>{websiteApi.about().then(setAbout).catch(()=>setAbout(null))},[]);return <div className="space-y-6"><div className="card p-8"><p className="text-sm font-bold text-cyan-700">About</p><h1 className="mt-2 text-4xl font-black">{about?.title || "About Muscab Gym"}</h1><p className="mt-4 max-w-4xl text-slate-600">{about?.description || "This React frontend is built for your Gym Management C# API. It provides public website pages plus full management screens for your database modules."}</p></div><div className="grid gap-6 md:grid-cols-2"><div className="card p-6"><h2 className="text-2xl font-black">Mission</h2><p className="mt-3 text-slate-600">{about?.mission || "To make gym operations faster, cleaner, and easier to manage."}</p></div><div className="card p-6"><h2 className="text-2xl font-black">Vision</h2><p className="mt-3 text-slate-600">{about?.vision || "To provide a professional digital platform for modern fitness centers."}</p></div></div>{about?.services?.length>0 && <div className="card p-6"><h2 className="mb-4 text-2xl font-black">Services</h2><div className="grid gap-3 md:grid-cols-2">{about.services.map(s=><div className="rounded-xl bg-slate-50 p-4 font-semibold" key={s}>{s}</div>)}</div></div>}</div>}
